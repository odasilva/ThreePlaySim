<Window x:Class="ThreePlaySim.SimulationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:custom="clr-namespace:ThreePlaySim"
        Title="SimulationView"
        WindowState="Maximized" >

    <Grid>
        <custom:Map x:Name="grid"  HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
        <TextBlock x:Name="TBNotification" HorizontalAlignment="Left" Margin="511,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="240" Height="415"/>
    </Grid>
</Window>
