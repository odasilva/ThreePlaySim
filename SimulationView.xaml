<Window x:Class="ThreePlaySim.SimulationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:custom="clr-namespace:ThreePlaySim"
        Title="SimulationView"
        WindowState="Maximized" >

    <Grid>
        <custom:Map x:Name="grid"  HorizontalAlignment="Left"  VerticalAlignment="Stretch"  Width="608"/>
        <TextBlock x:Name="TBNotification" HorizontalAlignment="Right"  Margin="10.4,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="124" Height="415"/>
    </Grid>
</Window>
